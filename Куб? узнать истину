var cubeChecker = function(volume, side) {
  if (volume <=0 || side <= 0)
   { 
  return false;}
  if ((Math.cbrt(volume)) !=side) {
  return false;}
  
  if (Number.isInteger(Math.cbrt(volume))) {
  return true;}
  {
     return false;
  }
};
